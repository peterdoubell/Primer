# Pinned so that a reinstall years from now reproduces a working book.
# Regenerate with: .venv/bin/pip freeze > requirements.lock.txt
fastapi==0.128.8
starlette==0.49.3
pydantic==2.13.4
pydantic_core==2.48.0
uvicorn==0.39.0
uvloop==0.22.1
httptools==0.8.0
websockets==15.0.1
watchfiles==1.1.1
python-dotenv==1.2.1
PyYAML==6.0.3
libzim==3.7.0
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.12.1
idna==3.19
certifi==2026.7.22
click==8.1.8
annotated-types==0.7.0
annotated-doc==0.0.5
typing_extensions==4.16.0
typing-inspection==0.4.2
exceptiongroup==1.3.1

# Cloud persistence. The book runs on local SQLite files and needs none of
# this to work; it is only used when TURSO_DATABASE_URL is set, which is how
# the hosted demo stops forgetting its reader between requests. libsql-client
# is the pure-Python libSQL client: no Rust toolchain, no compiled wheel to
# find, installs on every Python the rest of this file supports. (0.3.0, not
# 0.3.1: 0.3.1 declares its docs theme as a runtime dependency and drags
# Sphinx into every install.)
libsql-client==0.3.0
aiohttp==3.13.5
aiosignal==1.4.0
aiohappyeyeballs==2.6.1
attrs==26.1.0
frozenlist==1.8.0
multidict==6.7.1
propcache==0.4.1
yarl==1.22.0
async-timeout==5.0.1

# Google sign-in. Used only to verify a Google ID token's signature — the
# fetch of Google's own certificate is relayed through the httpx already
# pinned above (see server.py's _HttpxAuthTransport), so this pulls in a
# crypto backend, not a second HTTP client.
google-auth==2.50.0
cryptography==50.0.0
cffi==2.0.0
pycparser==2.23
pyasn1==0.6.4
pyasn1_modules==0.4.2

# Development / tests
pytest==8.4.2
