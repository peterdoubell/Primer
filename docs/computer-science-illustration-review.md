# Computer-science illustration review

Current scope: all 34 images inspected at 800px against lesson descriptions
and recorded. Computational Complexity now exposes exact growth counts and
three explicit Boolean clauses with a checkable satisfying assignment. Its
previous symbolic apparatus is recoverable from Git, but no longer serves as
the lesson illustration. This is a deliberate addition to the native renderer
inventory; the generator still protects other authored plates.
Per-lesson observations and responsive asset fingerprints are in
`illustration-reviews.json`.

Nine native plates received specific corrections: concrete procedure inputs,
unobscured block-code traces, a one-way CPU address bus with named ASCII
encoding, separate invalid-array storage, contained TLS prose, separated
merge-sort singletons, explicit live-status semantics, readable SQL result
labels, and a commit history that distinguishes commits from checks/references.

Metadata now matches six sorting objects and circle/triangle patterns. The CPU
path is explicitly not a raw keyboard protocol, and the Internet description
matches the actual routed-packet schematic. The Git example has one merge with
two real commit parents; test badges and main/HEAD do not masquerade as commits.

Existing correct worked examples were preserved: 1101 equals 13, the conditional
sum reaches 13, two doubling calls return 6 and 10, binary search checks
8/12/14/15/16, merge sort returns 1/2/5/7, SQL keeps Moss/Tide, and XOR/AND
form the complete half-adder table. Authored sandwich and stack/queue images
retain their concrete explanatory sequences.

Advanced review corrected two more native plates: encryption output prose now
fits outside its narrow tile, and training-loss/gradient arrows no longer cross
their labels. Dijkstra distances, all DFA transitions, SQL transfer totals,
held-out accuracy, normalized attention, typed reduction and Hadamard
interference were checked against their displayed worked examples.

## Verification and limits

Four new regressions inspect actual array-slot bounds, merge-sort tile bounds,
commit nodes/parents and memory-arrow direction/encoding. Two additional checks
cover encryption output layout and normalized attention/loss arithmetic. All
13 focused CS/review tests pass. All 74 language/CS illustration pairs pass the
generator check.

The current ledger is 231/432. This report does not certify enlarged browser
views, every model state, or the remaining subject
reviews. Final runtime/model checks and deployment are still unfinished.
